from setuptools import setup, find_packages

setup(
    name='sg2vid',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
    author='Ssharvien Kumar',
    description='SG2VID: Scene Graphs Enable Fine-Grained Control for Video Synthesis',
)